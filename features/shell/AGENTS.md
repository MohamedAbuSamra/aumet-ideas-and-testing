# Shell

Demo chrome around the prototype: sidebar, epic chips, phone bezel, website browser frame.

- `shell.html` — markup mounted into `#app`
- `shell.css` — layout for sidebar/stage/phone/web
- `shell.js` — element cache, chips, preview scale, keyboard/demo controls

Do not put product screens here. Those live under `features/mobile` or `features/website`.
