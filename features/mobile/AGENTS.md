# Mobile surface

Phone preview. Epics:

- `onboarding/` — registration and complete profile
- `login/` — log-in screen linked from landing

Shared visual chrome (create-account header, fields) is `shared/chrome.css`. Leftover unused UI helpers stay in `shared/legacy.css` until a screen needs them.

Route prefix: `#/mobile/<epic>/<screen>`.
