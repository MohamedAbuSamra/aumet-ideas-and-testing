# Login

Log-in HTML for the mobile phone. It is mounted next to onboarding screens and opened via `data-goto="login"`. It is not an onboarding stepper step.

Keep styles in `login/login.css`. Add `login.js` only if this screen needs behavior.
