# Dosage labels epic

Pulse POS print setup. Screens:

- `print-config/` — template configuration
- `dosage-library/` — saved dosage phrases
- `pos-index/` — POS cart + print dosage

Shared Pulse chrome and slip helpers: `shared/pulse.css`, `shared/dosage.js`.

`index.js` binds the active `data-feature-canvas` screen. Keep canvas ids in sync with `screens.js`.
